import styled from "styled-components";

const Logo = () => {
  return <LogoStyled />;
};

const LogoStyled = styled.div({
  width: "181px",
  height: "37px",
  backgroundColor: "#f1f2ae",
});

export default Logo;
